name: minimal-sandbox

hosts:
  - name: server
    base_box:
      image: debian-12-x86_64
      man_user: debian
    flavor: m1.tiny

networks:
  - name: server-switch
    cidr: 10.10.20.0/24

net_mappings:
  - host: server
    network: server-switch
    ip: 10.10.20.5

groups: []
